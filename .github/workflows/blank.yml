name: Cloud Video Streamer FIX
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Instalace KasmVNC a Desktopu
        run: |
          sudo apt update
          wget https://github.com/kasmtech/KasmVNC/releases/download/v1.3.1/kasmvncserver_bookworm_1.3.1_amd64.deb
          sudo apt install -y ./kasmvncserver_bookworm_1.3.1_amd64.deb
          sudo apt install -y xfce4 xfce4-goodies firefox
          
      - name: 2. Konfigurace KasmVNC (Bezpečné heslo)
        run: |
          mkdir -p ~/.vnc
          # KasmVNC používá yaml pro uživatele
          echo "kasmuid:
            password: $(openssl passwd -1 heslo123)
            permissions: [all]" > ~/.vnc/kasmvncuser.yaml

      - name: 3. Start KasmVNC a Tunelu
        run: |
          # Start serveru na portu 8443
          vncserver :1 -select-de xfce -Resolution 1280x720 -interface 127.0.0.1 -websocketport 8443 &
          
          # Cloudflare Tunel
          wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
          chmod +x cloudflared-linux-amd64
          ./cloudflared-linux-amd64 tunnel --url http://localhost:8443 > cf.log 2>&1 &
          
          sleep 20
          echo "-------------------------------------------------------"
          echo "ODKAZ PRO PLYNULÉ VIDEO (Přihlášení: kasmuid / heslo123):"
          grep -o 'https://[-0-9a-z]*\.trycloudflare\.com' cf.log
          echo "-------------------------------------------------------"

      - name: 4. Keep Alive
        run: sleep 21600
